@model ComicBookGallery.Models.Drawing[]
@{
    ViewBag.Title = "Drawing Register";
}

<h2>@ViewBag.Title</h2>

<div class="row">
    @foreach (var drawing in Model)
    {
        <div class="col-md-3">
            <h4>@Html.ActionLink(drawing.DisplayName, "Detail", new { id = drawing.Id })</h4>
        </div>
    }
    
</div>

